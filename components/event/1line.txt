横長のカードにするcss。
却下理由：下まで行くのが遠い





/* =========================
   Base Layout
========================= */
.eventcontent {
    display: flex;
    flex-direction: column;
    width: 100%;
    background-color: white;
    min-height: 100vh;
}

.toppers {
    padding-bottom: 20px;
}

.e-content {
    width: 100%;
}

/* =========================
   Search
========================= */
.e-search {
    position: relative;
    width: 60vw;
    max-width: 600px;
    min-width: 330px;
    margin: 0 auto;
}

.e-searchbox {
    width: 100%;
    padding: 10px 40px 10px 35px;
    border: 1px solid #ccc;
    border-radius: 25px;
    font-size: 16px;
    transition: border-color 0.3s, box-shadow 0.3s;
}

.e-searchbox:focus {
    outline: none;
    border-color: #0070f3;
    box-shadow: 0 0 5px rgba(0, 112, 243, 0.5);
}

.e-search::before {
    content: '🔍';
    position: absolute;
    left: 10px;
    top: 50%;
    transform: translateY(-50%);
    font-size: 18px;
    color: #888;
}

.e-clear {
    position: absolute;
    right: 10px;
    top: 50%;
    transform: translateY(-50%);
    background: transparent;
    border: none;
    font-size: 18px;
    cursor: pointer;
    color: #888;
}

/* =========================
   Icon Filter
========================= */
.e-iconbox {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    justify-content: center;
    margin: 20px 0;
}

.e-iconnomal {
    width: 100px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 2px solid transparent;
    border-radius: 20px;
    transition: transform 0.2s, border-color 0.2s;
    cursor: pointer;
}

.e-iconclick {
    background-color: #0070f3;
    color: #fff;
    border-color: #005bb5;
}

.e-iconunclick {
    background-color: #f5f5f5;
    color: #000;
}

/* =========================
   Event Grid (columns only switch)
========================= */
.e-eventbox {
    display: flex;
    justify-content: center;
    padding: 20px;
}

.e-eventbox-f {
    display: grid;
    gap: 1rem;
    width: 100%;
    grid-template-columns: 1fr; /* mobile default */
}

@media (min-width: 1100px) {
    .e-eventbox-f {
        grid-template-columns: repeat(2, 1fr);
    }
}

/* =========================
   Card (structure is always identical)
========================= */
.container {
    width: 100%;
}

.container .card {
    width: 100%;
    background: linear-gradient(to bottom left, #01b0d3, #14fc0a);
    border-radius: 12px;
    overflow: hidden;
    padding: 12px;
    transition: transform 0.15s ease, box-shadow 0.15s ease;
}

.container .card:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 20px rgba(0,0,0,0.18);
}

/* =========================
   Card Content Layout (always horizontal)
========================= */
.card-all {
    display: flex;
    flex-direction: row;
    gap: 12px;
}

/* Left image */
.pic {
    flex: 0 0 140px;
    height: 140px;
    overflow: hidden;
}

.piceve {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 10px;
}

/* Right side */
.card-right {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
}

.card-item-box h2 {
    font-size: 18px;
    font-weight: 600;
    margin: 0 0 6px 0;
    word-break: break-word;
}

/* Buttons */
.pop_button_area {
    display: flex;
    flex-direction: row;
    gap: 8px;
    margin: 6px 0;
}

.pop_button_sec {
    font-size: 12px;
    border-radius: 6px;
    border: 2px black solid;
    padding: 5px 8px;
    cursor: pointer;
    transition: background 0.15s ease, color 0.15s ease;
}

.pop_button_sec:hover {
    background-color: #0066ff;
    color: white;
}

/* Info text */
.card-about {
    font-size: 14px;
    opacity: 0.75;
}

/* =========================
   Empty Result
========================= */
.nothing {
    width: 100%;
    height: 260px;
    grid-column: 1 / -1;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    font-size: 1.2rem;
}
